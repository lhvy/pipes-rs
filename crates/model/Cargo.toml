[package]
edition = "2018"
license = "MIT OR Apache-2.0"
name = "model"
version = "0.1.0"

[dependencies]
anyhow = "1.0.37"
rng = {path = "../rng"}
serde = {version = "1.0.118", features = ["derive"]}
terminal = {path = "../terminal"}
tincture = "0.5.0"
